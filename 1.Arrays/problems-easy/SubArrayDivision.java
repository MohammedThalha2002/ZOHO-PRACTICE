public class SubArrayDivision {
    public static void main(String[] args) {
        int arr[] = { 1, 2, 1, 3, 2 };
        int win_size = 2;
        int val = 3;

        findValue(arr, win_size, val);
    }

    static int findValue(int[] arr, int win_size, int val) {
        int totalNum = 0;
        for (int i = 0; i < arr.length; i++) {
            if (i + win_size < arr.length) {
                
            }
        }
        return totalNum;
    }
}
